
/**
 * creates a url in the namespace of the material-design mod
 * @param name the trailing part of the name 
 */
export const createMDModNSUrl = (name: string): string => {
	return `metaexplorer.io/material-design/${name}`
}