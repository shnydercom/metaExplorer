{
  "private": true,
  "license": "EUPL",
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "workspaces": [
    "packages/*",
    "mods/*",
    "nocode/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "prestart": "lerna run build",
    "tsc": "lerna run tsc",
    "build": "lerna run build",
    "start": "set MIO_DYNAMIC=true && lerna run start"
  }
}
